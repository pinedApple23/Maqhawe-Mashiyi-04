const Repository = require('./Repository');

class ApplicationRepository extends Repository {
  // Extend in future (e.g., findByJobPostId, findByUserId)
}

module.exports = ApplicationRepository;
